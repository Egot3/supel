apiVersion: v1
kind: Secret
metadata:
  name: user-db-secret
  namespace: user-domain
type: Opaque
stringData:
  JWT_SECRET: a-string-secret-at-least-256-bits-long
  DB_USER: ets
  POSTGRES_PASSWORD: super_SECURE-pAsSwOrD2805!!!
  DB_HOST: user-db-0.user-db
  DB_PORT: "5432"
  DB_IDENTITY_NAME: user_db
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-db-init
  namespace: user-domain
data:
  init.sql: |
    CREATE ROLE ets WITH LOGIN SUPERUSER PASSWORD 'super_SECURE-pAsSwOrD2805!!!';

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db
  namespace: user-domain
spec:
  serviceName: user-db
  replicas: 1
  selector:
    matchLabels:
      app: user-db
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom:
            - secretRef:
                name: user-db-secret
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: user-db-data
              mountPath: /var/lib/postgresql/data
            - name: init-scripts-volume
              mountPath: /docker-entrypoint-initdb.d
      volumes:
        - name: init-scripts-volume
          configMap:
            name: user-db-init
  volumeClaimTemplates:
    - metadata:
        name: user-db-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 512Mi

---
apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: user-domain
spec:
  clusterIP: None
  selector:
    app: user-db
  ports:
    - port: 5432
