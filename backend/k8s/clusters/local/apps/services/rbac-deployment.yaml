apiVersion: apps/v1
kind: Deployment
metadata:
  name: rbac-service
  namespace: rbac-domain
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rbac-service
  template:
    metadata:
      labels:
        app: rbac-service
    spec:
      containers:
        - name: rbac-service
          image: ghcr.io/egot3/supel/backend/rbac:latest
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: rbac-db-secret
                  key: DB_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rbac-db-secret
                  key: POSTGRES_PASSWORD
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: rbac-db-secret
                  key: DB_PORT
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: rbac-db-secret
                  key: DB_USER
            - name: IDENTITY_SERVICE_HOST
              value: identity-service.identity-domain.svc.cluster.local
            - name: IDENTITY_SERVICE_PORT
              value: "50051"
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: rbac-service
  namespace: rbac-domain
spec:
  selector:
    app: rbac-service
  ports:
    - port: 50051
      targetPort: 50051
