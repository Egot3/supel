apiVersion: apps/v1
kind: Deployment
metadata:
  name: timetable-service
  namespace: timetable-domain
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timetable-service
  template:
    metadata:
      labels:
        app: timetable-service
    spec:
      containers:
        - name: timetable-service
          image: ghcr.io/egot3/supel/backend/timetable:latest
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: DB_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: POSTGRES_PASSWORD
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: DB_PORT
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: DB_USER
            - name: IDENTITY_SERVICE_HOST
              value: identity-service.identity-domain.svc.cluster.local
            - name: IDENTITY_SERVICE_PORT
              value: "50051"
            - name: USER_SERVICE_HOST
              value: user-service.user-domain.svc.cluster.local
            - name: USER_SERVICE_PORT
              value: "50051"
            - name: RBAC_SERVICE_HOST
              value: rbac-service.rbac-domain.svc.cluster.local
            - name: RBAC_SERVICE_PORT
              value: "50051"
            - name: STORAGE_HOST
              value: minio.storage-domain.svc.cluster.local
            - name: STORAGE_PORT
              value: "9000"
            - name: STORAGE_ACCESS_KEY
              value: minioadmin
            - name: STORAGE_SECRET_KEY
              value: minioadmin
            - name: STORAGE_NEWS_BUCKET
              value: news
            - name: STORAGE_PUBLIC_HOST
              value: "localhost"
            - name: STORAGE_PUBLIC_PORT
              value: "9000"
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: timetable-service
  namespace: timetable-domain
spec:
  selector:
    app: timetable-service
  ports:
    - port: 50051
      targetPort: 50051
