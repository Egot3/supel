apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints: [web]
  routes:
    - match: Host(`grafana.dev.local`) #rage inducting
      kind: Rule
      services:
        - name: grafana
          port: 3000
          namespace: monitoring
