apiVersion: apps/v1
kind: Deployment
metadata:
  name: timetable-service
  namespace: timetable-domain
spec:
  replicas: 1
  selector:
    matchLabels:
      app: timetable-service
  template:
    metadata:
      labels:
        app: timetable-service
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox
          command:
            - sh
            - -c
            - "until nc -z timetable-db-0.timetable-db 5432; do echo 'nothing ever happens(db)'; sleep 2; done"
      containers:
        - name: timetable-service
          image: ghcr.io/egot3/supel/backend/timetable:latest
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1000m"
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: DB_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: POSTGRES_PASSWORD
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: DB_PORT
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: DB_USER
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: timetable-db-secret
                  key: JWT_SECRET
            - name: STORAGE_HOST
              value: minio.storage-domain.svc.cluster.local
            - name: STORAGE_PORT
              value: "9000"
            - name: STORAGE_ACCESS_KEY
              value: minioadmin
            - name: STORAGE_SECRET_KEY
              value: minioadmin
            - name: STORAGE_TIMETABLE_BUCKET
              value: timetable
            - name: STORAGE_PUBLIC_HOST
              value: localhost
            - name: STORAGE_PUBLIC_PORT
              value: "9000"
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: timetable-service
  namespace: timetable-domain
spec:
  selector:
    app: timetable-service
  ports:
    - port: 50051
      targetPort: 50051
